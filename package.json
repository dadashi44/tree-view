{
  "name": "tree-view-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Монорепозиторий библиотеки @bigplay/tree-view",
  "workspaces": [
    "packages/core",
    "packages/vue3",
    "packages/vue2",
    "packages/nuxt",
    "playground"
  ],
  "scripts": {
    "dev": "npm run dev --workspace playground",
    "build": "npm run build --workspace @bigplay/tree-view-core && npm run build --workspace @bigplay/tree-view && npm run build --workspace @bigplay/tree-view-vue2 && npm run build --workspace @bigplay/tree-view-nuxt",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check:packaging": "node scripts/check-packaging.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run check:packaging",
    "set-version": "node scripts/set-version.mjs",
    "publish:all": "npm publish --workspace @bigplay/tree-view-core && npm publish --workspace @bigplay/tree-view && npm publish --workspace @bigplay/tree-view-vue2 && npm publish --workspace @bigplay/tree-view-nuxt"
  },
  "engines": {
    "node": ">=20.19"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dadashi44/tree-view.git"
  }
}
